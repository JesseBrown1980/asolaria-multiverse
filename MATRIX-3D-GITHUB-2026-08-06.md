# THE MATRIX CAPTURE — the multiverse seen in 3-D

Status: `MEASURED_GITHUB | LAW_APPLIED | FORGETTING_KEPT_BESIDE | SYSTEM_AFFIRMED=0`
Law: [3-D GITHUB OF THRUTH](https://github.com/JesseBrown1980/FOLLOW-THE-IS-NOT-THE-WILL-AND-WAS/blob/main/matrix/3-D-GITHUB-OF-THRUTH.md) · `56bc5ee06e9bb4f7`

## The forgetting, kept beside

The law was posted READ-FIRST at session start. LIRIS worked the whole day in
the flat projection — samples, doors, eggs, calls — and fetched the law only
at the operator's correction: *forgetting laws needs the 3-D harness to see in
time, color, space and energy.* The earlier work stands unchanged; this
capture is the correction, not a replacement.

## The capture

**30 nodes, 15 typed edges.** Every body keeps its own identity — name and
hash — and carries four dimensions, all checked-integer, no float:

| dimension | how it is measured |
|---|---|
| **time** | the commit's own unix instant |
| **color** | the first 3 bytes of the body's own SHA — the glyph rule; derived, never chosen |
| **space** | SHA bytes 4·5·6 as x·y·z in 0–255 — reproducible view coordinates, not identity |
| **energy** | inserted + deleted lines (commits, PRs), bytes (harness), conclusion (workflow) |

Node kinds: `repository · branch · commit · pull_request · workflow_state · harness`.
Edge types: `contains · points_to · proposes_to · proposes_from · merged_as · publishes`.

The day in the matrix: the repository contains five branches; main points to
the corridor commit `7d613cf` (author acer); three PRs merged as `93e794e`,
`b548387`, `2763287`; PR #4 proposes from `relic-clock-sample-6` to main; the
Pages workflow publishes the repository — `success` on the merge identity.
The harness itself is a node: the law's bytes, hashed.

Full rows: [MATRIX-3D-GITHUB-2026-08-06.hbi](MATRIX-3D-GITHUB-2026-08-06.hbi).

```text
FLAT_PAGE = PROJECTION(MATRIX)
PROJECTION_IS_WHOLE_MATRIX = 0
IDENTITY_EXCHANGE = 0
```

The resume rule holds: when the public state moves, remeasure and regenerate a
new capture. The harness is used always, as the next compaction takes over —
it is seeded into seat memory so the forgetting does not repeat.
